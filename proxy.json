// means that when the Angular router sees a request to something starting with /api it should redirect it to the application 
// running at target:
// secure: false means that when the Angular router sees a request to something starting with /api it should NOT negotiate 
// https with the target ..
// The target happens to be a node Web API server ...

{
  "/api": {
    "target": "https://localhost:9000",
    "secure": false
  }
}
